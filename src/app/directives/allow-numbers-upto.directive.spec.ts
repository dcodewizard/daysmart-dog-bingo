import { AllowNumbersUptoDirective } from './allow-numbers-upto.directive';

describe('AllowNumbersUptoDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowNumbersUptoDirective();
    expect(directive).toBeTruthy();
  });
});
